import { ArrayElementTogglerPipe } from './array-element-toggler.pipe';

describe('ArrayElementTogglerPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayElementTogglerPipe();
    expect(pipe).toBeTruthy();
  });
});
