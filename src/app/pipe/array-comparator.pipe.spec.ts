import { ArrayComparatorPipe } from './array-comparator.pipe';

describe('ArrayComparatorPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayComparatorPipe();
    expect(pipe).toBeTruthy();
  });
});
